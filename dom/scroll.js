// Scroll to Top
const scrollToTop = (el) => { el.scrollIntoView({ behavior: "smooth", block: "start" }); }

// Scroll to Bottom
const scgrollToBottom = (el) => { el.scrollIntoView({ behavior: "smooth", block: "end" }); }