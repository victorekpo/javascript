//EXAMPLE GULP FILE - gulpfile.js
var gulp = require('gulp');
gulp.task('default', async function() {  // gulp.task is called 'default', runs by default when gulp is executed. Next is the call back function to execute.
  console.log('hello from gulp');
});

